{% spaceless %}
    <div class="form-group row">
            <div id="news_files___name__" class="col-md-10 row">
                    <div class="form-group col-md-6 row">
                        {{ form_label(form.title, null, {"label_attr" : {"class": "col-md-4"}} ) }}
                        <div class="col-md-8">
                            {{ form_widget(form.title) }}
                        </div>
                    </div>
                    <div class="form-group col-md-6 row">
                        {{ form_label(form.file, null, {"label_attr" : {"class": "col-md-4"}} ) }}
                        <div class="col-md-8">
                            {{ form_widget(form.file) }}
                        </div>
                    </div>
            </div>
            <a href="#" class="btn btn-danger col-md-2 delete-btn">Supprimer</a>
    </div>
{% endspaceless %}